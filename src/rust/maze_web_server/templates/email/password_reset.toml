subject = "Reset your {{ app_name }} password"
text = """
{{ header }}

Hi {{ first_name }},

We received a request to reset the password on your {{ app_name }} account.
Click the link below to set a new password:

{{ reset_link }}

If you didn't request this, you can ignore this message — your password
won't change.

— {{ from_name }}

{{ footer }}
"""
html = """
{{ header }}
<p>Hi {{ first_name }},</p>
<p>We received a request to reset the password on your {{ app_name }} account.
Click the link below to set a new password:</p>
<p><a href="{{ reset_link }}">{{ reset_link }}</a></p>
<p>If you didn't request this, you can ignore this message — your password
won't change.</p>
<p>— {{ from_name }}</p>
{{ footer }}
"""
