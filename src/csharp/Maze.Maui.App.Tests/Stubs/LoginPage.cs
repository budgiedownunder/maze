// Stub of the MAUI ContentPage. LoginViewModel.cs has a `<see cref>`
// reference to LoginPage in its XML doc; the cref must resolve when the
// VM is file-linked into the non-MAUI test host. An empty class in the
// same namespace satisfies the compiler without pulling in any MAUI
// runtime. Same trick as Stubs/ChangePasswordPage.cs.
namespace Maze.Maui.App.Views
{
    internal sealed class LoginPage { }
}
