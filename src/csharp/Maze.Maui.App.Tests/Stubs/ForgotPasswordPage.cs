// Stub of the MAUI ContentPage. LoginViewModel uses
// `nameof(ForgotPasswordPage)` to build a Shell route — the symbol must
// resolve when the VM is file-linked into the non-MAUI test host. Same
// trick as Stubs/SignUpPage.cs.
namespace Maze.Maui.App.Views
{
    internal sealed class ForgotPasswordPage { }
}
