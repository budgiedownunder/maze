// Stub of the MAUI ContentPage. LoginViewModel uses
// `nameof(SignUpPage)` to build a Shell route — the symbol must resolve
// when the VM is file-linked into the non-MAUI test host. Same trick as
// Stubs/ChangePasswordPage.cs.
namespace Maze.Maui.App.Views
{
    internal sealed class SignUpPage { }
}
