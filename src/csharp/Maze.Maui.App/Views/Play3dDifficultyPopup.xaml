<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               x:Class="Maze.Maui.App.Views.Play3dDifficultyPopup"
               CanBeDismissedByTappingOutsideOfPopup="False">

    <Border Padding="20"
            StrokeShape="RoundRectangle 8"
            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray500}}"
            BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}">

        <VerticalStackLayout Spacing="12" MinimumWidthRequest="280">

            <!-- Title -->
            <Label Text="Choose Difficulty"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,0,0,4"/>

            <!-- Difficulty options (default: Easy) -->
            <VerticalStackLayout Spacing="8">
                <RadioButton x:Name="EasyRadio"   Content="Easy"   GroupName="play3dDifficulty" IsChecked="True"/>
                <RadioButton x:Name="TrickyRadio" Content="Tricky" GroupName="play3dDifficulty"/>
                <RadioButton x:Name="HardRadio"   Content="Hard"   GroupName="play3dDifficulty"/>
            </VerticalStackLayout>

            <!-- Buttons -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,4,0,0">
                <Button Grid.Column="0" x:Name="CancelButton" Text="Cancel" Clicked="OnCancelClicked"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                        TextColor="{AppThemeBinding Light={StaticResource Gray950}, Dark={StaticResource Gray100}}"/>
                <Button Grid.Column="1" x:Name="PlayButton" Text="Play" Clicked="OnPlayClicked"/>
            </Grid>

        </VerticalStackLayout>
    </Border>

</toolkit:Popup>
